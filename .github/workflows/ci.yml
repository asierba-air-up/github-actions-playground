name: test workflow

on: [push]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: netlify-cli  help
        run: netlify --help

      - name: netlify-cli which
        run: which netlify

