name: test workflow

on: [push]

jobs:
  build:
    runs-on: ubuntu-20.04

    steps:
      - name: netlify-cli  help
        run: netlify --help

      - name: netlify-cli which
        run: which netlify

